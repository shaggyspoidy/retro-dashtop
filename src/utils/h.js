import React from "react";

// Shorthand so component files read reasonably without a JSX build
// step: h(Box, {flexDirection: "row"}, child1, child2. ...)
export  const h = React.createElement;
export {React};
